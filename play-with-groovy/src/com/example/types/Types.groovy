package com.example.types



/*

// 1. Primitives

integral types: byte (8 bit), short (16 bit), int (32 bit) and long (64 bit)
floating-point types: float (32 bit) and double (64 bit)
the boolean type (one of true or false)
the char type (16 bit, usable as a numeric type, representing a UTF-16 code)

to convert/wrap primitives as objects with we use 'wrapper' classes

byte Byte
short Short
int Integer
float Float
double Double
char Character
boolean Boolean

imp-note : in groovy, by default, every thing ( even values ) are an object


2. Reference Types




 */



class Types {
    static void main(String[] args) {

        // primitives

        def i=12;
        def j=12.12;
        def k=12.12F;
        def l=12.12D;

        // references
        String movie="ps1"
        List<String> actors=['vikram','Nag']

    }
}
